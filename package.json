{
  "name": "frontend-game",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/setup-games.js && node scripts/generate-games-config.js && set PORT=3001 & next dev --turbopack",
    "build": "node scripts/setup-games.js && node scripts/generate-games-config.js && next build",
    "start": "next start",
    "lint": "next lint",
    "setup-games": "node scripts/setup-games.js",
    "discover-games": "node scripts/auto-discover-games.js",
    "generate-config": "node scripts/generate-games-config.js"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "mammoth": "^1.9.1",
    "next": "15.3.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4"
  }
}
